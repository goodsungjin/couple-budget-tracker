import { style } from '@vanilla-extract/css';

export const base = style([
  {
    padding: '24px 144px',
  },
]);

export const body = style([
  {
    overflowX: 'auto',
    overflowY: 'hidden',
    minWidth: 0,
  },
]);
